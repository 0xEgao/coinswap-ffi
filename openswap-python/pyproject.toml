[build-system]
requires = ["setuptools>=65.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "openswap"
version = "1.0.0"
description = "Openswap Taker bindings for Python"
readme = "README.md"
license = { text = "Apache-2.0 OR MIT" }
authors = [
    { name = "citadel-foss", email = "xyz@gmail.com" }
]
requires-python = ">=3.8"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries",
]

[project.urls]
Homepage = "https://github.com/citadel-foss/openswap"
Documentation = "https://citadel-foss.github.io/openswap-ffi/"
Repository = "https://github.com/citadel-foss/openswap-ffi.git"
Issues = "https://github.com/citadel-foss/openswap/issues"

[tool.setuptools]
packages = ["openswap"]
package-dir = { "" = "src" }

[tool.setuptools.package-data]
openswap = [
    "native/linux-x86_64/*.so",
    "native/linux-aarch64/*.so",
    "native/darwin-x86_64/*.dylib",
    "native/darwin-arm64/*.dylib",
    "native/win-amd64/*.dll",
]

[tool.setuptools.exclude-package-data]
openswap = ["*.py.bak"]
